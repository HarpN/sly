apiVersion: v1
kind: Service
metadata:
  name: {{ include "sly.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "sly.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app: sly-psn-sync
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
